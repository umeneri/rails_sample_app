<!-- objectはrenderのオプション引数で参照 -->
<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <!-- 単数形の名詞を複数形に変換する。  -->
      The form contains <%= pluralize(object.errors.count, "error") %>.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

