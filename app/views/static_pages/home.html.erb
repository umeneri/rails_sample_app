<% if logged_in? %>
  <div class="row">
    <%= render 'shared/sidebar' %>

    <div class="col-md-8">
        <h3>Micropost Feed</h3>
        <%= render 'shared/feed' %>
      </div>
    </div>

  </div>
<% else %>
  <%= render 'shared/signup' %>
<% end %>

